这是一份为你和你的搭档量身定制的“超微观”执行清单（To-Do List）。为了确保你们能完美覆盖理论课与6次实验的核心要求，并最终落地为一个具备商业级产品体验的A股招聘检索系统，我将整个软件工程的生命周期拆解成了极其细颗粒度的执行步骤。

你们可以在项目推进的过程中逐项打勾。遇到高算力需求的NLP任务（如模型微调或全量聚类），建议直接挂载到你那台跑得动Qwen-7B的服务器上执行；而在进行前端UI交互测试和本地代码调试时，你手边的高性能华硕ROG设备完全游刃有余。另外，每天晚上10点左右记得放下代码，准时去瓦罗兰特里放松一小时，保持劳逸结合才能高效产出。

以下是完整的项目执行清单：

### 阶段一：项目启动与数据基建 (Week 12)

* [ ] 1. 在本地电脑建立项目根目录 `A_Share_Job_IR_System`。
* [ ] 2. 初始化 Git 仓库 (`git init`)。
* [ ] 3. 与搭档明确前后端/算法分工（建议一人主攻后端与算法调度，一人主攻前端与PPT报告）。
* [ ] 4. 搭建 Python 虚拟环境 (`conda create -n ir_env python=3.9`)。
* [ ] 5. 安装核心依赖包 (`pip install pandas numpy scikit-learn jieba flask fasttext`)。
* [ ] 6. 准备 A 股招聘原始数据集（CSV 或 Excel 格式）。
* [ ] 7. 编写 `data_loader.py` 脚本读取原始数据。
* [ ] 8. 过滤冗余字段，仅保留：企业名称、股票简称、行业、城市、薪资、学历、经验、发布日期、职位描述。
* [ ] 9. 数据清洗：处理缺失值（Drop NaNs 或填充默认值）。
* [ ] 10. 编写正则化脚本，清除“职位描述”中的 HTML 标签、不可见字符和特殊符号。
* [ ] 11. 执行日期篡改逻辑：读取“发布日期”，利用 `datetime` 库自动计算并生成“篡改后结束日期”（发布日期 + 60天）。
* [ ] 12. 提取所有的 A 股公司名称和股票简称，构建专有实体词库。
* [ ] 13. 抽取行业分类字段，构建行业标签词库。
* [ ] 14. 抽取岗位名称字段，构建岗位 Title 词库。
* [ ] 15. 将清洗后的干净数据序列化保存为 `cleaned_jobs.json` 或存入轻量级数据库（如 SQLite）。

### 阶段二：底层核心算法实现与封装 (Week 12 - 13)

**实验 1-3 落地：分词与行业词典构建**

* [ ] 16. 创建 `nlp_pipeline` 文件夹。
* [ ] 17. 导入实验 3 中迭代优化后的 CRF 分词模型权重。
* [ ] 18. 在服务器上利用部分清洗后的“职位描述”语料，让 CRF 识别潜在的未登录词（OOV，如“电芯”、“高比能”）。
* [ ] 19. 人工审查 CRF 提取出的高频新词，剔除噪声。
* [ ] 20. 将上述词汇与步骤 12-14 的实体词库合并，生成 `A_share_finance_IT_dict.txt`。
* [ ] 21. 编写 `tokenizer.py`，配置机械分词引擎（如加载自定义词典的 jieba ）。
* [ ] 22. 实现双向最大匹配（BiMM）或直接调用已加载自定义词库的分词引擎对“职位描述”进行全量切词。
* [ ] 23. 编写脚本剔除常见停用词（“的”、“了”、“以及”）。

**实验 4 落地：无监督关键词抽取**

* [ ] 24. 编写 `keyword_extractor.py`。
* [ ] 25. 计算全量语料的文档频率（DF）并生成逆文档频率（IDF）字典。
* [ ] 26. 实现 TF-IDF 算法，并为岗位 Title 中出现的词汇设置 2.0 倍的权重加成。
* [ ] 27. 实现 TextRank 算法，设置滑动窗口大小为 5。
* [ ] 28. 遍历所有清洗后的数据，利用上述算法为每条岗位抽取 Top-5 “核心技能标签”。
* [ ] 29. 将抽取出的标签数组追加到清洗后的数据集中（新增一列 `core_tags`）。

**实验 5 落地：文本自动打标（分类）**

* [ ] 30. 如果原始数据缺失统一的粗粒度岗位类别（如“研发”、“销售”），编写 `classifier.py`。
* [ ] 31. 从数据中抽样一部分，人工标注其粗粒度类别作为训练集。
* [ ] 32. 提取训练集 JD 的 Top-200 TF-IDF 稀疏向量特征。
* [ ] 33. 训练线性支持向量机（SVM）或朴素贝叶斯模型。
* [ ] 34. 保存训练好的模型（`job_classifier.pkl`）。
* [ ] 35. 用该模型对全量数据进行推理，自动补全或校准所有数据的分类标签。

**实验 6 落地：无监督关联聚类**

* [ ] 36. 编写 `cluster_engine.py`。
* [ ] 37. 基于 TextRank 提取的特征，将全量岗位向量化。
* [ ] 38. 在服务器上运行 K-Means 算法（尝试 K=10 或 20 个大簇）。
* [ ] 39. 将每条岗位数据所属的 Cluster ID 追加到数据集中。
* [ ] 40. 预先计算簇内岗位之间的余弦相似度，生成“相似岗位关联表”（用于产品端的“猜你喜欢”功能）。

### 阶段三：产品设计与开题汇报提案 (Week 14 - 核心里程碑：6月2日)

**战略层与范围层设计**

* [ ] 41. 确定系统产品名为“A股精聘检索系统”或类似名称。
* [ ] 42. 梳理系统目标（System Goal）：融合课程实验，实现结构化+语义检索的精准匹配。
* [ ] 43. 梳理用户目标（User Goal）：求职者快速定位高薪且技能匹配的A股岗位。
* [ ] 44. 构思 1-2 个用户画像（Persona，如应届理工科毕业生）。
* [ ] 45. 确定必须实现的功能：主检索、条件过滤、标签展示、相似推荐。

**结构层与框架层设计**

* [ ] 46. 使用工具（如墨刀、Axure 或手绘）绘制“信息架构图”（展示页面层级）。
* [ ] 47. 绘制首页（Home）低保真线框图：居中大搜索框 + 热门搜索词标签。
* [ ] 48. 绘制结果页（SERP）线框图：左侧过滤面板，右侧瀑布流列表，突出薪资与提取的技能标签。
* [ ] 49. 绘制详情页（Detail）线框图：左侧长文本 JD，右侧展示聚类相似岗位。

**开题 PPT 制作 (5-8分钟)**

* [ ] 50. PPT 页面规划（建议控制在 10-12 页以内，切忌花哨）。
* [ ] 51. Slide 1: 封面（课题名称、两人姓名）。
* [ ] 52. Slide 2: 语料来源与特征（展示 A 股招聘数据样条及规模）。
* [ ] 53. Slide 3: 语料预处理规范（说明怎么篡改了日期、清洗了标点）。
* [ ] 54. Slide 4: 战略层设计（展示 User Persona 和痛点）。
* [ ] 55. Slide 5: 系统处理链路（核心图示，串联实验 1-6 的逻辑流）。
* [ ] 56. Slide 6: 结构层与框架层展示（贴上线框图）。
* [ ] 57. Slide 7: 团队分工与后续 15-16 周的开发排期。
* [ ] 58. 组内预演，严格将讲解时间控制在 7 分钟左右。
* [ ] 59. 导出为 PDF。
* [ ] **60. 【截止点：6月2日课前】在课程中心提交开题汇报 PPT。**
* [ ] **61. 【里程碑】完成课堂 5-8 分钟开题汇报。**

### 阶段四：后端引擎搭建与倒排索引 (Week 14 - 15)

* [ ] 62. 在本地项目中建立 `backend` 目录。
* [ ] 63. 初始化 Flask 或 FastAPI 应用框架。
* [ ] 64. 构建倒排索引构建器 `indexer.py`。
* [ ] 65. 遍历处理好的全量数据集，对 `企业名称`、`行业`、`职位描述` 分词后的结果建立倒排表 (Inverted Index)。
* [ ] 66. 实现词项词典 (Term Dictionary) 存储每个词的文档频率。
* [ ] 67. 将倒排索引序列化到磁盘（加载入内存以备快速检索）。
* [ ] 68. 编写向量检索逻辑 `search_engine.py`。
* [ ] 69. 接收自然语言 Query 并进行分词。
* [ ] 70. 依据倒排表快速召回候选文档。
* [ ] 71. 计算 Query 与候选文档的向量空间模型（VSM）余弦相似度分数。
* [ ] 72. 编写排序函数 `ranker.py`。
* [ ] 73. 引入结构化过滤条件（城市、学历、行业）在召回阶段进行数据切片。
* [ ] 74. 引入“时间衰减因子”（利用篡改后的结束日期，当前时间越接近结束日期，稍微降低权重或打上“Urgent”标签）。
* [ ] 75. 综合计算最终得分并返回 Top-K 结果。
* [ ] 76. 设计并开发 API: `/api/hot_tags` (返回 TF-IDF 全局高频词用于首页展示)。
* [ ] 77. 设计并开发 API: `/api/search` (接收关键词和过滤条件，返回 JSON 格式的排序岗位列表)。
* [ ] 78. 设计并开发 API: `/api/detail/<job_id>` (返回某个岗位的详细原始文本及属性)。
* [ ] 79. 设计并开发 API: `/api/recommend/<job_id>` (基于聚类结果，返回同一簇内的 Top-3 相似岗位)。
* [ ] 80. 使用 Postman 或 cURL 测试所有后端接口的数据联通性。

### 阶段五：前端表现层开发 (Week 15)

* [ ] 81. 在根目录建立 `frontend` 目录。
* [ ] 82. 使用 Vue.js / React 或纯 HTML+原生 JS 初始化页面结构。
* [ ] 83. 设置表现层规范：确定系统的基准字体、商务色彩（深蓝/白/灰）、圆角风格等。
* [ ] 84. **首页开发**：实现顶部居中布局，添加输入框组件。
* [ ] 85. 请求 `/api/hot_tags`，将热词渲染在输入框下方。
* [ ] 86. 实现回车或点击按钮触发搜索，并进行页面跳转（携带 URL 参数）。
* [ ] 87. **检索结果页开发**：实现整体网格/流式布局。
* [ ] 88. 搭建左侧筛选栏组件，渲染城市、行业、学历的复选框（Checkbox）。
* [ ] 89. 实现薪资区间的滑动条组件（Slider）。
* [ ] 90. 监听筛选栏的状态变化，动态拼接参数再次请求 `/api/search`。
* [ ] 91. 搭建右侧结果列表组件，循环渲染后端返回的 JSON 列表。
* [ ] 92. 在列表卡片中，对匹配到的关键词进行标红（高亮）渲染。
* [ ] 93. 在卡片醒目位置，用小徽章（Badge）的 UI 形式渲染实验 4 抽取出来的“技能标签”。
* [ ] 94. 编写逻辑：判断岗位结束日期，若在 15 天内，则渲染红色的“即将截止”图标。
* [ ] 95. 实现分页加载（或无限滚动加载）功能。
* [ ] 96. **岗位详情页开发**：实现两栏布局。
* [ ] 97. 解析并渲染原始的岗位描述文本，保持换行格式。
* [ ] 98. 请求 `/api/recommend/<job_id>` 接口。
* [ ] 99. 将返回的相似岗位渲染在侧边栏或底部区域。
* [ ] 100. 增加“返回结果列表”的导航按钮。
* [ ] 101. 声明并区分 AI 生成代码：在代码注释中明确标注哪些前端组件是 AI 辅助生成的，哪些是自行手写的。

### 阶段六：系统集成、评测与结项展示 (Week 16 - 核心里程碑：6月16日)

* [ ] 102. 本地同时启动前端服务器与后端 Flask/FastAPI 引擎。
* [ ] 103. 测试极端情况下的检索（输入极其偏门的词汇、复杂的长句），修复可能导致引擎崩溃的 Bug。
* [ ] 104. 检查各种筛选组合（如上海+本科+10k薪资）是否准确生效。
* [ ] 105. 准备评测基准：挑选 10-20 个有代表性的查询短语（Queries）。
* [ ] 106. 在自己开发的系统中搜索这些 Queries，记录返回的前 20 条结果。
* [ ] 107. 团队内部进行人工标注：判断这 20 条结果与查询词的相关度（0 或 1）。
* [ ] 108. 依据理论课第 5 章，计算系统的查准率（Precision）和查全率（Recall）。
* [ ] 109. 计算平均查准率（MAP）以及 F-measure 值。
* [ ] 110. 将评测数据导出为表格形式，备用于最终报告。
* [ ] 111. 准备结项展示脚本（5分钟无PPT纯演示）。
* [ ] 112. 演练场景 1：展示首页，输入自然语言查询，展示检索速度与分词高亮效果。
* [ ] 113. 演练场景 2：展示结构化过滤，演示漏斗效应，重点强调技能标签。
* [ ] 114. 演练场景 3：点入详情页，展示基于聚类的“相似职位”推荐逻辑。
* [ ] 115. 在 ROG 电脑上录制一段 3 分钟的系统操作备用视频，以防现场演示时出现网络或服务宕机。
* [ ] **116. 【里程碑：6月16日】课堂上进行 5 分钟结项汇报系统展示。**

### 阶段七：实验报告撰写 (Week 17 - 最终收尾)

* [ ] 117. 创建 Word 文档，设置标准的论文格式体系（标题、摘要、目录、正文、附录）。
* [ ] 118. 第一章：引言（阐述 A 股招聘系统的选题背景与建设意义）。
* [ ] 119. 第二章：系统分析与用户研究（贴入前期做的人物画像、体验地图等设计图稿）。
* [ ] 120. 第三章：数据收集与预处理（描述冗余剔除、日期字段设计等数据清洗过程）。
* [ ] 121. 第四章：核心算法实现（最重点篇幅。将前述的 N-gram、混合分词、TF-IDF/TextRank 抽词、分类、K-Means 聚类理论逐一说明其在系统中的落地作用）。
* [ ] 122. 第五章：系统设计与实现（阐述倒排索引架构，区分人工手写算法部分与 AI 参与的前端实现部分）。
* [ ] 123. 第六章：系统测评（贴入第 108-110 步计算出来的性能评测 2×2 表格及 F1 值，分析系统的优势与不足）。
* [ ] 124. 第七章：总结与展望。
* [ ] 125. 检查字数与篇幅控制：严格确保正文不超过 30 页，总字数在 10000 - 15000 字左右（最高不超过 2 万字）。
* [ ] 126. 将核心代码片段、全量标签数据样表等舍不得删减的心血材料，全部整理进“附录”中。
* [ ] 127. 检查文档逻辑是否连贯，避免单纯的“123”罗列。
* [ ] 128. 生成 PDF 格式，等待教务通知最终提交时间。
* [ ] 129. 正式提交最终实验报告。
* [ ] 130. 庆祝期末项目顺利完结。