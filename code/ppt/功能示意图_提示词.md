# 上市公司招聘信息检索系统 —— 功能示意图生成提示词

> 风格要求：学术论文插图风、白色背景、中文界面、信息图/示意图、微软雅黑、无照片无装饰
> 图片比例：16:9

---

## F1 · 多字段联合检索（单搜索框 + 自动字段权重）

```
一张学术论文风格的搜索界面示意图，展示用户从输入关键词到看到结果的完整流程。白色背景，纵向布局。

上半部分（约1/3高度）是搜索区：
- 顶部居中一个宽大的搜索输入框，框中显示已输入的关键词"Python 开发"
- 搜索框右侧有一个蓝色"搜索"按钮
- 搜索框下方一行小字说明："输入岗位/技能/公司名，系统自动加权排序"
- 再下方是4个并排的轻量快捷筛选：行业[不限▾] 城市[不限▾] 学历[不限▾] 薪资[不限▾]

中间有一条浅灰色分隔线，线上标注"找到 128 条结果，用时 0.23s"

下半部分（约2/3高度）展示3张纵向排列的职位卡片，每张卡片包含：
- 左侧：一个醒目的"相关度 0.87"得分（蓝色大字）
- 中间：岗位名（加粗）、企业名 + 股票简称（如"宁德时代 300750"）、📍上海 · 💰¥20k-35k · 🎓本科
- 右侧：一段职位描述片段，其中"Python"和"开发"用亮黄色背景高亮
- 三张卡片的相关度依次递减：0.87 → 0.62 → 0.45

右下角有一个小虚线框，框内标注文字"后台自动加权：招聘岗位词×2.0 | 职位描述词×1.0 | 企业名精确匹配加分"。框用浅灰色虚线边框，表示这是后台逻辑而非界面元素。

底部标注"排序方式：相关度 · 余弦相似度降序"。

右上角标签"VSM + TF-IDF 多字段权重融合"。

线条图风格，无照片，微软雅黑字体，配色干净克制。
```

---

## F2 · 结构化筛选（布尔逻辑过滤）

```
一张学术论文风格的检索过滤示意图。白色背景，左右两栏布局。左栏（约1/3宽度）垂直排列6个筛选条件，每个用浅灰线分隔：（1）行业：3个已勾选的多选框（计算机通信、软件信息、互联网）；（2）城市：下拉菜单显示"上海"；（3）学历：单选按钮"本科"已选中；（4）经验：下拉菜单显示"3年及以上"；（5）薪资：双滑块，左端标注"10k"右端标注"50k"；（6）发布日期：显示"最近一周"。左栏底部有一个"应用筛选"按钮。右栏（约2/3宽度）顶部标注"筛选前：5,000条 → 筛选后：128条"，下方展示3张缩小后的职位卡片。右上角标签"布尔逻辑 AND 组合过滤"。学术插图风格。
```

---

## F3 · 关键词高亮（查询词精准标记）

```
一张关键词高亮效果示意图。白色背景，展示一段约8行的中文职位描述原文（模拟真实招聘文本）。文本中的"Python""数据分析""机器学习"三个查询词用亮黄色背景（#FFF176）高亮显示并加粗为深灰色。其余文字为正常黑色。文本左对齐，行距1.8倍。段落上方标注小字"查询词："Python""数据分析""机器学习" → 3处匹配"。文本右侧用浅灰色竖线标注行号（L1-L8）。底部标注"前端 JS 正则匹配 → <mark> 标签包裹"。右上角标签"全文关键词高亮"。学术论文插图风格，无照片。
```

---

## F4 · 多维度排序（前端本地重排）

```
一张排序功能示意图。白色背景，水平排列4个排序选项按钮："相关度排序（选中，蓝色高亮）""发布日期 ↓""最低月薪 ↓""最高月薪 ↓"。按钮下方展示排序切换前后的对比。左侧标注"排序前·按相关度"，5条职位卡片（仅显示岗位名+相关度得分）。中间一个浅蓝色箭头标注"切换为 · 按最低月薪降序"。右侧标注"排序后·按月薪"，同样的5条职位卡片按最低月薪从高到低重新排列（月薪数值加粗显示）。底部标注"前端本地排序，不重新查询后端"。右上角标签"客户端多维排序"。学术信息图风格。
```

---

## F5 · 9 行业标签浏览（NB 文本分类驱动）

```
一张行业分类功能示意图。白色背景，上半部分展示3×3网格排列的9个行业标签卡片，每个圆角卡片上标注行业名（如"互联网/软硬件技术""金融""生产制造/汽车"等），右下角小字标注该行业职位数（如"1,200条""580条"）。其中"互联网/软硬件技术"卡片用蓝色边框高亮表示"用户悬停"。下半部分展示分类流程：左侧"400条人工标注"→ 中间"NB vs KNN 对比评测（NB 微 F1=25% 胜出）"→ 右侧"NB 对全量 5,000 条预测行业标签"。三个步骤用浅色箭头连接。右上角标签"NB 文本分类 · 9 大行业"。学术论文插图风格。
```

---

## F6 · 技能族群聚类（K-Means 词汇聚类）

```
一张词汇聚类示意图。白色背景，中央展示10个浅色圆角矩形块，排列为2行5列，代表10个技能族群。每个块内用3-5个关键词填充（字号不一，模拟词云），块标题分别为："设备制造""采购管理""HR招聘""生产质量""研发投资""财务审计""电商运营""食品安全""数据AI""客户销售"。每个块右下角标注词数（如"85词""132词"）。图下方左侧标注"方法：K-Means (K=10) + 等级聚类 (Ward/Single/Complete)"，右侧标注"评估：簇间分离度 0.78 · 簇内紧密度 0.15"。底部标注"800高频词 × 5000文档词-文档矩阵 → L2归一化聚类"。右上角标签"K-Means 词汇聚类 · 技能族群发现"。学术信息图风格。
```

---

## F7 · 相似岗位推荐（TF-IDF 余弦相似度）

```
一张相似岗位推荐示意图。白色背景，纵向布局。顶部是一张较大的"当前职位"卡片（岗位名"Python后端开发工程师"、企业名、月薪、城市）。卡片右侧标注"TF-IDF向量"。下方标题"相似岗位推荐（余弦相似度 Top-5）"，水平排列5张较小的卡片。每张卡片顶部标注相似度百分比（"0.34""0.32""0.30""0.28""0.27"），下方是岗位名和企业名。其中相似度最高的卡片用浅蓝色边框高亮。卡片之间有一个小标注"计算方式：当前职位向量 × 5000 文档矩阵 → cos = A·B/(|A||B|)"。右上角标签"TF-IDF 余弦相似度推荐"。学术论文风格。
```

---

## F8 · 关键词抽取（TF-IDF + TextRank 双算法 Pooling）

```
一张关键词抽取示意图。白色背景，左右对比布局。左栏标题"TF-IDF 抽取（F1=44.61%）"，下方展示约10个关键词标签（浅蓝色圆角边框，字号不一），大号标签包括"风险管理""Python""信贷"，小号标签包括"数据分析""风控"。右栏标题"TextRank 抽取（F1=41.92%）"，同样展示关键词标签但顺序不同。中间竖线分隔，标注"Pooling 融合 → 取并集"。底部展示"最终 Top-15 关键词标签（详情页展示，点击可触发搜索）"。底部左下角标注"TF-IDF: P=32.97% R=76.11% | TextRank: P=30.87% R=72.82% | 100条人工金标准评测"。右上角标签"TF-IDF + TextRank 关键词抽取"。学术信息图风格。
```

---

## F9 · 数据统计仪表盘（多维度可视化）

```
一张数据统计仪表盘示意图。白色背景，2×2网格布局展示4张图表：（左上）行业招聘数量横向柱状图，Top-5行业标注数值；（右上）城市招聘热度柱状图，Top-5城市标注数值；（左下）薪资分布直方图，X轴标注5k/10k/15k/20k/25k/30k/40k/50k+，柱体使用浅蓝色；（右下）学历要求分布饼图，分为本科/硕士/大专/博士/不限五块。四张图共用统一的浅蓝色配色，各图左上角有灰色小字图题（如"图1 行业招聘数量Top-5"）。右上角标签"多维度数据可视化"。学术论文风格。
```

---

## F10 · 分词效果对比（7种分词器评测弹窗）

```
一张分词对比示意图。白色背景，纵向布局。顶部是一行原文："负责嵌入式软件开发与调试，熟悉C语言与RTOS系统"。下方展示4行分词结果对比，每行前标注分词器名称和F1值：（1）"CRF（F1=74.77%）"用蓝色标注，分词：负责/嵌入式/软件/开发/与/调试/熟悉/C语言/与/RTOS/系统；（2）"Ngram（F1=73.43%）"；（3）"FMM（F1=66.46%）"；（4）"HMM（F1=67.06%）"。不同分词方案之间的差异位置用浅红色下划线标注（如CRF分出"嵌入式"而FMM分出"嵌入/式"）。底部有一个小表格：

| 模型 | P(%) | R(%) | F1(%) |
| CRF | 78.76 | 71.16 | 74.77 |
| Ngram | 78.43 | 69.04 | 73.43 |
| FMM | 70.98 | 62.49 | 66.46 |

右上角标签"7种分词器评测对比 · BIES+集合双评测"。学术论文插图风格。
```

---

## 通用要求

- 图片比例：16:9
- 背景：纯白色 #FFFFFF
- 中文字体：微软雅黑
- 配色：主色 #1a73e8（蓝），高亮 #FFF176（黄），边框 #e0e0e0（浅灰），文字 #202124
- 线条图风格 · 无照片 · 无3D · 无阴影 · 无渐变
